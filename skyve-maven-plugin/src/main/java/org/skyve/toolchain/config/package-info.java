/**
 * Provides mutable parameter beans consumed by the Skyve Maven mojos.
 *
 * <p>These types are populated by Maven's parameter binding and expose no behaviour beyond storing and
 * normalising configuration values.
 */
package org.skyve.toolchain.config;