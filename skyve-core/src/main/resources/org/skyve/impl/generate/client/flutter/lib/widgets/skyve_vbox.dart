import 'package:flutter/material.dart';

class SkyveVBox extends Wrap {
  SkyveVBox({Key? key, List<Widget> children = const []})
      : super(key: key, direction: Axis.vertical, children: children);
}
