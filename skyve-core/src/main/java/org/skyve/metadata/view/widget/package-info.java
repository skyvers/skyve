/**
 * Widget metadata interfaces for Skyve view declarations.
 *
 * <p>This package contains the declarative metadata contracts for view widget
 * configuration. Currently the key type is {@link org.skyve.metadata.view.widget.FilterParameter},
 * which represents a pre-filter constraint applied to query-backed widgets (lists,
 * lookups, etc.) before they render.
 *
 * <p>Widget implementations are in the {@code skyve-ext} and {@code skyve-web} layers;
 * this package contains only the structural metadata contracts.
 *
 * @see org.skyve.metadata.view.widget.bound
 */
package org.skyve.metadata.view.widget;
