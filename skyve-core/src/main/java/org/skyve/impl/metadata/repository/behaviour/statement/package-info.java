/**
 * JAXB-annotated base for individual behaviour script statement steps.
 *
 * <p>Contains {@code StatementMetaData} — the abstract JAXB root element for a single
 * statement in a behaviour action script (invoke, set, or if). Concrete subtypes are
 * in the parent package ({@link org.skyve.impl.metadata.repository.behaviour}) and
 * the runtime execution model is in
 * {@link org.skyve.impl.metadata.behaviour}.
 */
package org.skyve.impl.metadata.repository.behaviour.statement;
