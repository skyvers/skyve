/**
 * Named reusable view fragments (components) that can be referenced from multiple views.
 *
 * <p>{@code Component} represents a named view component definition (an {@code .xml}
 * view file registered as a component). {@code ComponentViewVisitor} traverses the
 * component's view element tree. {@code ComponentNameMap} maintains the registry of
 * component names to their resolved definitions within a customer context.
 *
 * @see org.skyve.impl.metadata.view
 */
package org.skyve.impl.metadata.view.component;
