/**
 * Layout container implementations for the Skyve view metadata model.
 *
 * <p>Containers group and arrange child view elements within a rendered view. This
 * package provides implementations for:
 * <ul>
 *   <li>{@code VBox} / {@code HBox} — vertical and horizontal flex-style boxes.
 *   <li>{@code Tab} / {@code TabPane} — tabbed panels grouping content by tab.
 *   <li>{@code Collapsible} — an expandable/collapsible section.
 *   <li>{@code Sidebar} — a pinned side-panel container.
 * </ul>
 *
 * <p>The {@code form} sub-package contains the specialised form-layout containers.
 *
 * @see org.skyve.impl.metadata.view.container.form
 * @see org.skyve.metadata.view.container
 */
package org.skyve.impl.metadata.view.container;
