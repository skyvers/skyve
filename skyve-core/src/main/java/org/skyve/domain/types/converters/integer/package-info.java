/**
 * {@link java.lang.Integer} and {@link java.lang.Long} converters for numeric formatting.
 *
 * <p>Converters in this package format integer values as human-readable strings:
 * <ul>
 *   <li>{@code IntegerConverter} — formats a plain integer with no grouping.
 *   <li>{@code IntegerSeparator} — formats an integer with thousands-separator grouping.
 *   <li>{@code LongIntegerConverter} — formats a long integer with no grouping.
 *   <li>{@code LongIntegerSeparator} — formats a long integer with thousands-separator grouping.
 *   <li>{@code SimplePercentage} — formats an integer as a percentage string (appends %).
 * </ul>
 *
 * @see org.skyve.domain.types.converters
 */
package org.skyve.domain.types.converters.integer;
