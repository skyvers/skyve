/**
 * Converter for Skyve dynamic enumerations.
 *
 * <p>{@code DynamicEnumerationConverter} converts between a dynamic enumeration code
 * (the stored string value) and its description (the display string) using the
 * document's metadata at conversion time. This supports enumerations whose members are
 * not static Java enums but are instead declared in document attribute metadata.
 *
 * @see org.skyve.domain.types.converters
 */
package org.skyve.domain.types.converters.enumeration;
