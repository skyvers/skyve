package modules.admin.Jobs;

import org.skyve.util.test.SkyveFactory;

/**
 * Declares default test factory settings for the jobs admin document.
 */
@SkyveFactory(testAction = false)
public class JobsFactory {
	// builder defaults
}
