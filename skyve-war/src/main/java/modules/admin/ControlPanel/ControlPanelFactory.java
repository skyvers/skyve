package modules.admin.ControlPanel;

import org.skyve.util.test.SkyveFactory;

/**
 * Creates Control Panel document instances with default runtime values.
 */
@SkyveFactory(testAction = false)
public class ControlPanelFactory {
	// builder defaults
}
