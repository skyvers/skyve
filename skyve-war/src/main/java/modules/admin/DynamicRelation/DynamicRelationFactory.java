package modules.admin.DynamicRelation;

import org.skyve.util.test.SkyveFactory;

/**
 * Declares factory metadata for the abstract dynamic-relation document.
 */
@SkyveFactory(testDomain = false, testAction = false)
public class DynamicRelationFactory {
	// Switch off test generation as this document is abstract
}
