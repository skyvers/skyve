package modules.admin.JobSchedule;

import org.skyve.util.test.SkyveFactory;

/**
 * Declares default test factory settings for job schedule document generation.
 */
@SkyveFactory(testDomain = false, testAction = false)
public class JobScheduleFactory {
	// builder defaults
}
