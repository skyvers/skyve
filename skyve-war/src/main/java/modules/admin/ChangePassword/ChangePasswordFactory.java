package modules.admin.ChangePassword;

import org.skyve.util.test.SkyveFactory;

/**
 * Creates Change Password documents for the current user context.
 */
@SkyveFactory(testAction = false)
public class ChangePasswordFactory {
	// builder defaults
}
