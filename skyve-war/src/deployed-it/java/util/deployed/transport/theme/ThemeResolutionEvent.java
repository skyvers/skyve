package util.deployed.transport.theme;

/** Event vocabulary owned by the deployed theme lifecycle suite. */
public enum ThemeResolutionEvent {
	SELECTION_MARKER_REMOVED,
	MODEL_UPDATE,
	ACTION_INVOKED
}
