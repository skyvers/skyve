package util.sail.commands;

/**
 * Marker interface for commands representing radio interactions in the UI test framework.
 * <p>
 * Implementations encapsulate parameters required to perform radio actions on different UI technologies.
 */
public interface RadioCommand {

	int index();
}
