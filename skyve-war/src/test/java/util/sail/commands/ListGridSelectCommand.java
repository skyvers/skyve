package util.sail.commands;

/**
 * Marker interface for commands representing list grid select interactions in the UI test framework.
 * <p>
 * Implementations encapsulate parameters required to perform list grid select actions on different UI technologies.
 */
public interface ListGridSelectCommand {
	// Nothing more to see here
}
