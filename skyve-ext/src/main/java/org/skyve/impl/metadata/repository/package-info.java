/**
 * Extended metadata repository implementations for Skyve (skyve-ext layer).
 *
 * <p>{@code DefaultRepository} is the production repository that loads and
 * caches module and document metadata from the configured metadata source.
 * {@code LocalDataStoreRepository} extends it to support local file-system
 * metadata discovery.
 *
 * @see org.skyve.impl.metadata.repository
 */
package org.skyve.impl.metadata.repository;
